# HANDOFF - 2026-04-30

## 使用ツール
Claude Code（Mac アプリ・Sonnet 4.6）

## 作業対象プロジェクト
AnimaWorks — Kyoka事業部 P2完了・P3待ち

---

## 本日の達成

### 1. scenario_004 動画投稿・scenario_005 画像作成
- scenario_004「梅雨の縁側に端居す」: 動画作成済み・TikTok + Instagram 投稿済み ✅
- scenario_005「月明かりの床の間に座す」: 画像作成済み ✅（動画・投稿は次回）
- 両シナリオの Notion ステータスを chisuke が更新済み

### 2. GPT Image Prompt 参照画像アンカー修正（004・005）
前セッションで「Notion更新済み」と報告されていたが、実際には未反映だった。
今セッションで両シナリオの Notion `GPT Image Prompt` フィールドに以下を追加し、fetch で反映確認済み：
- `⚠️⚠️⚠️ MANDATORY IMAGE GENERATION MODE ⚠️⚠️⚠️` ヘッダー
- `[Reference image uploaded: portrait of a Japanese woman named Kyoka]`
- `CRITICAL — FACE CONSISTENCY: Every frame MUST feature the identical face as the reference image`

### 3. P2 コミット & push 完了
```
commit: 74bec9e1
push: fork/main
対象: core/tools/kyoka_image.py / _kyoka_prompt_template.py / kyoka_pipeline.py / .agent/handoff/
```

### 4. メモリ・教訓の記録
- `memory/feedback_external_write_verify.md` を新規作成
- MEMORY.md に「外部サービス書き込み後は必ず fetch で確認してから完了と報告する」ルールを追記

---

## 現在のタスクと進捗

| タスク | 状態 |
|--------|------|
| P1（プロンプト基盤） | ✅ 完了 |
| P2（kyoka_pipeline・Notion登録） | ✅ 完了・push済み |
| P3（rin/kiri/sumi 自律パイプライン稼働） | ⬜ 未着手 |
| scenario_005 動画化・投稿 | 🟡 画像済み・動画未 |
| scenario_002 投稿（2本目） | ⬜ 未着手 |

---

## 次のセッションで最初にやること

### 1. scenario_005 を Seedance で動画化 → 投稿（chisuke 手動）
- Seedance Prompt は Notion の scenario_005 レコードにあり
- 投稿後に Notion の `Posting Status` / `Posted At` を更新

### 2. P3 着手（rin/kiri/sumi 自律パイプライン稼働）
詳細は `~/.claude/plans/c-ok-anima-dynamic-blum.md` 参照。

優先順：
1. rin / kiri / sumi に `heartbeat.md`（cicchi 構造を踏襲）作成
2. rin の knowledge 初版: `weekly_strategy.md` / `theme_registry.md` / `tiktok_log.md`
3. kiri の knowledge 初版: `market_pulse_kyoka.md`
4. `shared/blackboard/kyoka_status.md` + `blackboard_writer.py` 拡張
5. rin / kiri / sumi の `injection.md` / `cron.md` / `permissions.md` を P3 用に更新
6. `status.json` 全員 `enabled: true`
7. lint + cron パース + サーバー再起動 + 動作確認

---

## 注意点・ブロッカー

### GPT Image Prompt の参照画像アンカー
- **今後生成する全シナリオに必須**: `_kyoka_prompt_template.py` の `MANDATORY_HEADER` / `MANDATORY_FACE_CONSISTENCY` が Notion に正しく入っているかを毎回 fetch で確認すること
- kyoka_pipeline が生成するプロンプトは `MANDATORY_HEADER` を自動付与する設計になっているが、手動修正時は要注意

### コスト管理
- kyoka_pipeline（LLM）: 1シナリオ約 $0.02
- 画像生成: ChatGPT GUI（月額プラン範囲内）
- gpt-image-1 API（kyoka_image.py）は保持するが呼び出さない

### Anthropic API キー
- `.env` と `~/.animaworks/config.json` の両方を更新すること（2026-04-28 更新済み）
- 次回ローテート時は**両方**更新

### AIの報告を鵜呑みにしない
- Notion 等外部サービスへの書き込みは、AI が「完了」と言っても目視確認を習慣にすること

---

## 関連ファイル

| ファイル | 内容 |
|---------|------|
| `~/.claude/plans/c-ok-anima-dynamic-blum.md` | P2/P3 全体実装計画 |
| `core/tools/_kyoka_prompt_template.py` | キャラ仕様・プロンプト正規化（画像品質の要） |
| `core/tools/kyoka_pipeline.py` | シナリオ生成 → Notion 登録 |
| `core/tools/kyoka_image.py` | gpt-image-1 ラッパー（将来用・現在未使用） |
| `~/.animaworks/animas/cicchi/heartbeat.md` | P3 で rin 用 heartbeat の雛形 |
| Notion Kyoka Scenarios DB | https://www.notion.so/f1d407fcb3d94ca78cd81ddbe2c11d67 |
