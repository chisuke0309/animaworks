# HANDOFF - 2026-04-24 午後

## 使用ツール
Claude Code (Opus 4.7 / 1M context)

## 作業対象プロジェクト
AnimaWorks — cicchi HB 4段化 + ブラックボード導入（成長実感改善の実装フェーズ）

---

## 重要な方針決定

### 問題意識
- chisukeが「Animaが成長している実感がない」と指摘
- 核心: 組織目標（goals.md）がプロンプトに入っているのに、意思決定に使われていない。cron起動 → 過去ログ模倣 → 投稿 の自動化スクリプト状態
- Claude Code が持つ Plan / TodoWrite / Verify / Reflect のような「考える質」が欠落

### 3層コミュニケーション設計を合意
1. **層1: ブラックボード**（共有状況ファイル）← 今回実装
2. 層2: Inbox（現行 task_queue + Inbox、将来フォーマット厳格化）
3. 層3: ミーティング（週1で supervisor + 配下が議論）← 将来

### かんばん方式についての結論
- Anima分業は維持（人格の継続性・非同期組織感・観測性）
- 但し各Animaの頭の中を Claude Code 化する（4段フロー + Subagent呼出可）

---

## 今回実装した内容（全8ステップ完了）

### コード変更
- [x] `core/_anima_heartbeat.py`: Per-Animaテンプレ解決、Contract抽出、blackboardローダー、regex拡張（5段対応）
- [x] `core/tools/x_search.py`: `get_user_metrics()` メソッド追加（hana由来の流用）
- [x] `core/tools/blackboard_writer.py`: 新規作成（X API → goals.md → pace計算 → organization_status.md をアトミック書込）
- [x] `templates/ja/prompts/heartbeat.cicchi.md`: cicchi専用5段テンプレ（Observe → Plan → Execute → Verify → Reflect + Contract）
- [x] `templates/ja/prompts/fragments/blackboard_header.md`: ブラックボード注入時のヘッダ

### 設定変更
- [x] `~/.animaworks/shared/blackboard/organization_status.md`: X API実値で自動生成（現在フォロワー9人、目標100人、残91人、15.17人/day必要）
- [x] `~/.animaworks/shared/blackboard/market_pulse.md`: 将来用stub
- [x] `~/.animaworks/animas/cicchi/cron.md`: 7:00 / 21:00 に `blackboard_update_org_status` cron追加
- [x] `~/.animaworks/animas/cicchi/heartbeat.md`: checklist先頭に3項目追加（ブラックボード優先・Contract再読・goals確認）
- [x] `AGENTS.md`: ブラックボード / per-Animaテンプレ / Contract 規約を明記

### maru チーム停止（cicchi検証に専念するため）
- [x] maru / chiro / tama の `status.json` を `enabled: false` に変更
- [x] サーバー再起動（launchctl kickstart）で反映

---

## 試したこと・結果

### ✅ 成功
- **実機X API取得成功**: TrinityDox_JP のフォロワー数が9（前ハンドオフ時7→2日で+2の進捗確認）
- **ブラックボードのE2E動作**: goals.md パース → pace計算 → アトミック書き込みまで確認
- **統合テスト全項目パス**: テンプレ解決・フォールバック・Contract/Reflection抽出・regex剥がしすべて動作
- **blackboard_writerの自動登録**: `core/tools/` に置くだけで TOOL_MODULES に認識される
- **実装順序の工夫**: 各ステップを独立検証可能にしたことでリスクを分散

### ❌ 過去の失敗から学んで回避
- **regex拡張を必ずセット**: MEMORY.md記載のフィードバックループ問題を回避。5段全ヘッダ（Observe/Plan/Execute/Verify/Reflect/Contract）を剥がす設計
- **Contractは episodes に書かない**: activity_log のみに保存して翌日HBに直接注入（RAG経由しない）
- **Notionスキーマ変更は作り直しが早い**: 前回 Kyoka DB作業での教訓を本件では回避（DBではなくファイル方式）

---

## 次のセッションで最初にやること

### Codex バグレビュー（最優先）
Claude Code 再起動後、Codex MCP (`mcp__codex__*`) が使えるようになる。
以下のレビュー依頼書をそのまま Codex に投入する。

**Codex への依頼書（A案：今回の変更に絞る）:**

```markdown
# Bug Review: cicchi HB 4段化 + ブラックボード実装

## スコープ
以下の変更ファイルに絞って、実装バグ・論理ミス・既存機能への回帰リスクを指摘してほしい。
提案はせず、バグ指摘に集中すること。

## 変更ファイル（2026-04-24 変更）
- core/_anima_heartbeat.py（既存修正）
  - `_resolve_heartbeat_template_name()` 新規
  - `_load_latest_contract()` 新規
  - `_load_blackboard_snapshot()` 新規
  - `_build_background_context_parts()` にブラックボード注入を追加
  - `_build_heartbeat_prompt()` にテンプレ解決・Contract ブロック注入を追加
  - `_execute_heartbeat_cycle()` の summary regex 拡張（Observe/Plan/Execute/Verify/Reflect/Contract）
  - `_extract_contract()` + `_RE_CONTRACT` 新規、活動ログ `heartbeat_contract` タイプ
- core/tools/x_search.py（既存修正）
  - `XSearchClient.get_user_metrics()` 新規
- core/tools/blackboard_writer.py（新規）
  - goals.md パース → X APIフォロワー取得 → pace計算 → atomic write
  - cron `type: command` から呼ばれる tool
- templates/ja/prompts/heartbeat.cicchi.md（新規）
  - cicchi専用5段テンプレ + Contract
- templates/ja/prompts/fragments/blackboard_header.md（新規）
- ~/.animaworks/animas/cicchi/cron.md（7:00/21:00 blackboard_update_org_status 追加）
- ~/.animaworks/animas/cicchi/heartbeat.md（checklist先頭に3項目追加）
- AGENTS.md（規約追記）

## レビュー観点（優先順）

1. **フィードバックループ再発**: regex が5段全ヘッダを剥がせるか、境界ケース（見出しレベル違い・全角スペース・絵文字混入）で漏れないか
2. **Contract抽出の誤作動**: `[CONTRACT]` タグが reflection と混在した時の挙動、空block時の空文字伝播、複数blockがある時の挙動
3. **blackboardローダーの堅牢性**: ディレクトリ非存在・パーミッションエラー・巨大ファイル・バイナリ混入時の挙動、合計32KB上限の切り方
4. **テンプレ解決のフォールバック**: `load_prompt` のキャッシュが FileNotFoundError をキャッシュしてしまうリスク、format_map 失敗時のフォールバック
5. **blackboard_writer のレースコンディション**: atomic write は正しく tmp→rename しているか、失敗時にstale content を残さないか
6. **X API エラー処理**: 401/403/429/500系の分岐、`get_user_metrics` が `data` キー無しで空dict返すケース
7. **既存Animaへの無回帰**: cicchi以外のAnimaでも `yesterdays_contract_block` プレースホルダ不一致が発生しないか
8. **タスクキュー保守契約**: 新フローでも `auto_block_stale_tasks()` → `auto_resolve_old_tasks()` → `maybe_compact()` の3点セットが維持されているか

## 出力形式
各指摘につき:
- 重大度: 🔴Critical / 🟡Major / 🟢Minor
- 場所: ファイル:行番号
- 問題: 1〜2文で簡潔に
- 再現条件: どういう状況で発生するか
- 修正案: コード断片不要、方針のみ

バグがなければ「該当なし」と明記すること。
```

### Codex レビュー後にやること

1. **cicchi HB の実機動作確認**: `animaworks anima trigger cicchi heartbeat` で新プロンプトが正しく組み立てられるか確認。prompt_logs/ を開いてブラックボード・Contract枠が入っているか目視

2. **1日目の観察**: 翌朝（4/25 8:00 / 9:00 / 10:00）の cicchi 動作を見て、episodes に「フォロワー9人、目標100まで残91人」のような数値言及が出現するかを確認

3. **Contract carry-over 検証**: HB 2回目以降に `{yesterdays_contract_block}` が埋まるか確認（初回HB後に1日待つ必要あり）

4. **他Anima無回帰確認**: rue / kuro / sora / hana / yomi の prompt_logs が従来と byte-identical か、または差分が「ブラックボード挿入のみ」であることを確認（blackboardは全Animaに注入される設計）

---

## 注意点・ブロッカー

### 既知の制約・リスク
- **ブラックボードは全Animaに注入される**: cicchi以外の Anima（rue/kuro/yomi等）の Observe にも `organization_status.md` が入る。当面は無害だがプロンプトが数百バイト膨らむ。1週間運用で影響を観察
- **X API レート**: `GET /2/users/by/username/*` のレートは緩いが、7:00 / 21:00 の2回/日で十分。無駄に頻度を上げないこと
- **フィードバックループ再発リスク**: heartbeat.cicchi.md に新セクションを足すなら `_execute_heartbeat_cycle()` L470-475 の regex を必ず同時に拡張する（AGENTS.md記載）

### 新規運用ルール（AGENTS.md反映済）
- ブラックボードの書き込みは担当Animaを1つに固定する（同一ファイルに複数Animaが書かない）
- per-Animaテンプレはファイル追加だけで有効（`heartbeat.<name>.md`）
- Contract は activity_log の `heartbeat_contract` タイプのみ参照する

### maru チーム停止中
- maru / chiro / tama の3体は `enabled: false` で完全停止
- TikTok自動投稿・スクレイピング・カルーセル制作はすべて止まっている
- cicchi検証効果確認後、同じ 4段+ブラックボード パターンを maru チームに適用する方針
- 復帰手順: 3ファイルの `enabled: false` → `true` + `launchctl kickstart -k gui/$(id -u)/com.animaworks.server`

### 他事業部
- **Kyoka事業部**（rin/kiri/sumi）: 停止中（4/22ハンドオフ時から変わらず）。Kyoka Scenarios DB と3シナリオは健在、次回は素材量産・投稿戦略の設計
- **yomi**（CrowdWorks巡回）: 毎日20:03で正常稼働中
