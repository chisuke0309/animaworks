# HANDOFF - 2026-03-10

## 作業対象プロジェクト
AnimaWorks（/Users/chisuke/Projects/animaworks）

## 今日の作業まとめ

### 1. X API 認証問題の解決（OAuth 1.0a 401 エラー）

- **根本原因**: `.env` の Consumer Secret が X Developer Console の値と不一致
- **解決**: OAuth 1.0 キーを全て再発行し `.env` を更新
  - `TWITTER_API_KEY=REDACTED_TWITTER_API_KEY`
  - `TWITTER_API_SECRET=REDACTED_TWITTER_API_SECRET`
  - `TWITTER_ACCESS_TOKEN=REDACTED_TWITTER_ACCESS_TOKEN`
  - `TWITTER_ACCESS_TOKEN_SECRET=REDACTED_TWITTER_ACCESS_TOKEN_SECRET`
  - Bearer Token も URL デコード済みで更新
- **動作確認**: tweet_id `2031188888501497912` の投稿成功を確認

### 2. X投稿パイプライン実装（cicchi cron）

cicchi の `cron.md` に2タスクを追加：

| タスク | スケジュール | 内容 |
|--------|------------|------|
| X投稿：AIトレンド発信 | 毎日 12:00 | web_search→スレッド作成→x_post_thread→ログ記録 |
| X投稿：エンゲージメント計測 | 毎日 18:00 | x_search→likes/RTs/impressions を x_post_log.md に記録 |

週次振り返り（木曜 17:00）に x_post_log.md の14行超過分を削除する処理も追加。

### 3. 記憶・学習ループの設計と実装

コンテキストウィンドウ肥大化防止のため **ローリングウィンドウ方式**を採用：

- `knowledge/x_post_log.md` — 生データ（最大14行）。週次で古い行を削除
- `knowledge/x_strategy.md` — 蒸留された戦略メモ（効いたパターン・フック傾向等）
- 投稿時は x_strategy.md のみ参照（コンパクト）

### 4. システムプロンプト最適化（3つの改善）

#### 4-1. モデルアップグレード（rue / kuro）
- `gemini/gemini-3.1-flash-lite-preview` → `gemini/gemini-2.5-flash-lite`
- `~/.animaworks/animas/rue/status.json` と `kuro/status.json` を更新

#### 4-2. Priming ノイズ除去（`excluded_tools`）

**`core/config/models.py`** — `PrimingConfig` に `excluded_tools: list[str]` フィールドを追加：
```python
excluded_tools: list[str] = [
    "search_memory", "read_memory_file", "write_memory_file",
    "archive_memory_file", "read_file", "write_file", "edit_file",
    "list_directory", "search_code", "read_channel", "read_dm_history",
    "discover_tools", "refresh_tools",
]
```

**`core/memory/priming.py`** — フィルタを `format_for_priming` 呼び出し前に適用：
```python
if self._excluded_tools:
    prioritized = [
        e for e in prioritized
        if not (e.type == "tool_use" and e.tool in self._excluded_tools)
    ]
```

#### 4-3. Anthropic Prompt Caching（`anthropic_fallback.py`）

**前提**: `claude_agent_sdk` が未インストール → cicchi/sora は `AnthropicFallbackExecutor` を使用

**`core/execution/anthropic_fallback.py`** に `_build_cached_system()` 関数を追加：
- `# 3. 現在の状況` / `# 3. Current Situation` でプロンプトを分割
- Groups 1+2（静的）に `cache_control: {type: ephemeral}` を付与
- `create_kwargs["system"]` と `stream_kwargs["system"]` に適用
- 効果: 静的部分（キャラクター定義・ルール等）のトークン最大90%削減

### 5. Permissions 最適化

- `rue/permissions.md`: 外部ツールを `web_search + x_search` のみに絞り込み
- `kuro/permissions.md`: 外部ツールを `web_search` のみに絞り込み

---

## チーム構成（現在）

```
ちっち / cicchi（マネージャー・claude-sonnet-4-6）
├── くろ / kuro（ライター・gemini/gemini-2.5-flash-lite）
├── ルー / rue（リサーチャー・gemini/gemini-2.5-flash-lite）
└── そら / sora（ビジュアルディレクター・claude-haiku-4-5-20251001）
```

---

## 既知の課題・未完了事項

| 課題 | 状態 |
|------|------|
| X投稿 12:00 cron の実動確認（投稿・ログ記録） | 要確認（次の12:00に自動実行） |
| X投稿 18:00 cron の実動確認（エンゲージメント計測） | 要確認 |
| x_search が Bearer Token Free tier で使えるか | `GET /2/users/:id/tweets` は要確認 |
| dispatch.py zen-saha ブランチ → PR #1 マージ | https://github.com/chisuke0309/animaworks/pull/1 |
| sora の fal.ai ツール群（tiktok_visual.py 等）が未設定 | 要再作成 |
| identity.md が各 anima にない | 不要か確認 |

---

## 次のセッションで最初にやること

1. X投稿が 12:00 に正常投稿されたか `x_post_log.md` を確認
2. エンゲージメント計測（18:00）が動いているか確認
3. x_search の Free tier 制限を調査（必要なら有料プランへの移行を検討）
4. PR #1（dispatch.py fix）をマージ

---

## 参照ファイル

| ファイル | 変更内容 |
|---------|---------|
| `/Users/chisuke/Projects/animaworks/.env` | X API 認証情報を更新 |
| `~/.animaworks/animas/cicchi/cron.md` | X投稿2タスク追加・週次振り返り更新 |
| `~/.animaworks/animas/cicchi/knowledge/x_post_log.md` | 新規作成（ローリングウィンドウ投稿ログ） |
| `~/.animaworks/animas/cicchi/knowledge/x_strategy.md` | 新規作成（蒸留戦略メモ） |
| `~/.animaworks/animas/rue/status.json` | モデル更新 |
| `~/.animaworks/animas/kuro/status.json` | モデル更新 |
| `~/.animaworks/animas/rue/permissions.md` | 外部ツールを web_search+x_search に絞り込み |
| `~/.animaworks/animas/kuro/permissions.md` | 外部ツールを web_search に絞り込み |
| `core/config/models.py` | PrimingConfig に excluded_tools を追加 |
| `core/memory/priming.py` | excluded_tools フィルタを適用 |
| `core/execution/anthropic_fallback.py` | _build_cached_system() でプロンプトキャッシュ実装 |
