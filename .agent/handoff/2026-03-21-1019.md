# HANDOFF - 2026-03-21 (セッション13)

## 使用ツール
Claude Code (Antigravity)

## 作業対象プロジェクト
AnimaWorks（/Users/chisuke/Projects/animaworks）

## セッション13で完了した作業

| 対応 | 内容 |
|------|------|
| ✅ **Rule of Two セキュリティ評価** | NVIDIAの"Rule of Two"フレームワークに対するAnimaWorksの現状評価を実施。強み・弱みを整理 |
| ✅ **Rule of Two 実装（改善案A）** | `core/tooling/handler.py` に `_rule_of_two_used` トラッキングと `handle()` ガードを追加。`web_fetch` と `execute_command` がサイクル内で同時使用された場合、後から呼ばれた方を `RuleOfTwoViolation` エラーでブロック。`reset_session_id()` でサイクル開始時に自動リセット |
| ✅ **テスト追加（6件）** | `tests/unit/tooling/test_handler.py` に `TestRuleOfTwo` クラスを追加。6/6 passed |
| ✅ **カスタマイズまとめ作成** | 本家2026-03-01リリースからの変更14コミット分を `docs/customization_summary.md` に整理 |
| ✅ **LaunchAgent 自動起動確認** | `state=running`・ログから 2026-03-21 07:49:05 にブート後自動起動を確認。`RunAtLoad=true` / `KeepAlive=true` 設定済み。エラーなし。**課題クローズ** |


### 製品コンセプト確定
**「Telegramから指示するだけで動く、その会社専用のAI社員」**

3本柱：
1. **SNS運用**（cicchi→rue→kuro）
2. **市場調査**（rue→kuro）
3. **御社専用カスタマイズ**（Empty Vessel × 会社ポリシー注入）

### Go-To-Market戦略
- Phase 0（今）: 自分で使う・動作実証
- Phase 1（近い）: 1〜2社に無料β提供、Mac miniのまま手作り運用
- Phase 2: 収益化 → クラウド移行・マルチテナント化

### インフラの現実
- 現在: Mac mini 上で動作（再起動でサービス断リスクあり）
- マルチカンパニー化は将来課題（1体あたり~200-500MB消費）
- **最初の数社は手作り運用で十分。まず売れるか検証**

### 参考記事（ビジネス検証）
チューリング社のPRマネージャーが「32人の仮想PRチーム」をClaude Codeで構築。
→ AnimaWorksは「これを買うだけで使える商品」として提供できる。

### Notion戦略ページ（作成予定）
- 場所: Notion「ビジネス」配下
- 内容: 製品コンセプト・差別化要因・競合調査結果
- 市場調査はrueに委託予定

## LaunchAgent 現状

- **ラッパースクリプト**: `/Users/chisuke/.animaworks/scripts/server_start.sh`
- **plist**: `/Users/chisuke/Library/LaunchAgents/com.animaworks.server.plist`
- **次回ブート時に自動起動するか検証が必要**
- **手動起動**: `launchctl kickstart -k gui/$(id -u)/com.animaworks.server`

## 現在のAnima構成

| Anima | モデル | 役割 |
|-------|--------|------|
| cicchi | claude-haiku-4-5-20251001 | パイプライン管理・X投稿指揮 |
| rue | gemini/gemini-3.1-flash-lite-preview | リサーチ |
| kuro | gemini/gemini-3.1-flash-lite-preview | 執筆 |
| sora | claude-haiku-4-5-20251001 | 画像生成・待機中に自律改善 |

## 残課題

### 優先度高
| 課題 | 状態 |
|------|------|
| **Notionビジネス戦略ページ作成** | 未着手。rueに市場調査委託も |
| **SNS運用代行β提供の1社目開拓** | 知人企業に声かけ |

### セキュリティ改善残件（将来実装予定）

> **改善A（Rule of Two）は実装完了。下記B〜Eは将来対応。**

| # | 改善案 | 要点 | 優先度 |
|---|--------|------|--------|
| B | **`web_fetch` 安全注意の強化** | 現在の `read_file` 同様に「プロンプトインジェクションリスクあり」の警告を `web_fetch` の返答にも付与する | 中 |
| C | **`rue` の `execute_command` 制限** | rue は現在 `execute_command` を持つが、ほぼ用途がない。ツールスキーマから削除するか、rue の `permissions.md` でコマンドセクションを空にする | 中 |
| D | **Telegram 受信メッセージのバリデーション** | `server/telegram_handler.py` に入力長制限と制御文字フィルタを追加し、Telegram経由のプロンプトインジェクションリスクを低減 | 低（現状は内部利用のみ） |
| E | **`execute_command` のサンドボックス化** | Phase 2（クラウド移設時）に Docker or `firejail` でコマンド実行を隔離。ローカルMac運用中は対応コスト大のためリスク受容中 | Phase 2 |

| 課題 | 判断 |
|------|------|
| duplicate content 再トリガー | heartbeat.md で暫定対処済み。リスク受容 |
| task_queue 自動cleanup | 手動対応可。リスク受容 |
| activity_log ローテーション | 毎朝05:00 JST に自動実行済み。問題なし |

### 収益化ロードマップ
| 機能 | 状態 |
|------|------|
| Telegram入力受信 | ✅ 実装・動作確認済み |
| Notionへの戦略蓄積 | 🔄 着手待ち |
| SNS運用代行β | 🔄 1社目を探す段階 |
| Stripe連携 | ❌ 未着手 |

## Git リモート管理

```
origin  = https://github.com/xuiltul/animaworks.git （本家・pull only）
fork    = git@github.com:chisuke0309/animaworks.git （自分のfork・push先）
```

## 注意事項
- **ハンドオフにAPIキー等の機密情報を書かない**（値は `.env` 参照）
- X投稿は有料会員（25,000字）前提で1投稿フォーマット運用中
- cicchiのheartbeat.mdは夜間自己改善ループで自律更新（毎晩23:30）
- gemini-3.1-flash-lite-preview は多段ツール呼び出しが安定動作確認済み
- cicchi inbox は**ディレクトリ**（heartbeat.md に明示済み）
