# HANDOFF - 2026-03-12 夕

## 使用ツール
Claude Code

## 作業対象プロジェクト
AnimaWorks（/Users/chisuke/Projects/animaworks）

## 現在のタスクと進捗

- [x] サーバー再起動（複数回）
- [x] 全Animaの「悪い記憶」クリーンアップ（episodes/vectordb/activity_log全日付）
- [x] `core/_anima_heartbeat.py` に heartbeat_end サマリー切り詰め修正
- [x] `core/_anima_heartbeat.py` に heartbeat_reflection 保存条件を修正
- [ ] MEMORY.md の「実装済みの修正」セクションが古い記述を含んでいる → 要修正
- [ ] 19:00 パイプライン実行確認（本日中）

## 今回判明した問題と対処

### 根本原因：heartbeat_end フィードバックループ（完全版）

```
LLM verbose出力（Observe/Plan/Reflect）
→ heartbeat_end.summary に丸ごと保存
→ _load_heartbeat_history() で次回HBのプロンプトに注入
→ LLMが同形式を真似る → ループ
```

前回のセッションで「activity_logクリーンアップ済み」と報告したが、**episodes/ と vectordb/ が未対応**だった。さらに今回、activity_logのクリーンアップをしても**新しいHBが悪いheartbeat_endエントリを書き込む**ため、クリーンアップだけでは根本解決にならないことが判明。

### 修正内容（`core/_anima_heartbeat.py`）

**heartbeat_end保存（L340付近）**:
```python
# 保存前にサマリーを正規化
_hb_summary = result.summary or ""
if "HEARTBEAT_OK" in _hb_summary:
    _hb_summary = "HEARTBEAT_OK"
elif _hb_summary:
    import re as _re
    _hb_summary = _re.sub(
        r"##\s*(?:Observe|Plan|Reflect)(?:（[^）]*）)?\b.*",
        "", _hb_summary, flags=_re.DOTALL | _re.IGNORECASE,
    ).strip()[:200] or _hb_summary[:200]
self._activity.log("heartbeat_end", summary=_hb_summary)
```

**heartbeat_reflection保存（L372付近）**:
```python
# アクションがあった場合のみ保存（HEARTBEAT_OK = アクションなし → 保存しない）
if reflection_text and len(reflection_text) >= _MIN_REFLECTION_LENGTH and _hb_summary != "HEARTBEAT_OK":
```

### クリーンアップの完全手順（MEMORY.md更新済み）

前回セッションの手順に追加:
- **episodes/** → 全削除（search_memoryの主要ソース）
- **vectordb/** → 全削除してディレクトリ再作成（ChromaDB）
- **index_meta.json** → `{}` にリセット
- **shortterm/chat/archive/** → 全削除
- **heartbeat_reflection** typeのエントリは削除（heartbeat_end/heartbeat/llm_responseはHEARTBEAT_OK置換）

## ⚠️ 次のセッションで要対応：MEMORY.mdの修正

`/Users/chisuke/.claude/projects/-Users-chisuke-Projects-animaworks/memory/MEMORY.md` の「実装済みの修正」セクション（L174-183）に誤った情報が残っている：

```
**実装済みの修正（2026-03-12）**:
- ...: heartbeat_reflection 保存時にO/P/R形式・OVERDUE等の悪パターンを含む場合は保存しないチェックを追加  ← 古い・誤った実装説明
```

実際の最終実装は「OPRパターン判定」ではなく「**_hb_summary != "HEARTBEAT_OK"** かどうか（アクション有無）で判定」に変更済み。MEMORY.mdのこの説明を正確に更新すること。

## 試したこと・結果

- ✅ activity_log（全日付）+ episodes + vectordb + shortterm の全削除 → search_memoryから過去の悪い記憶が消える
- ✅ heartbeat_end サマリー正規化コード → HB履歴への汚染を防ぐ
- ✅ heartbeat_reflection 保存条件をアクション有無で判定 → 恣意的パターン判定を廃止
- ❌ activity_logのみのクリーンアップ（初回）→ episodes/vectordbが残存し再発
- ❌ heartbeat_reflection に悪パターンキーワードリストで判定（中間実装）→ 恣意的で設計が悪い、廃止

## 次のセッションで最初にやること

1. **MEMORY.mdの「実装済みの修正」セクションを正確な実装内容に更新**
2. **次のHBサイクル（:00/:05/:10/:15）でHEARTBEAT_OK 1行のみが出ることを確認**
3. **19:00パイプライン実行結果の確認**（cicchi cron → rue research → kuro writing → cicchi post）

## 注意点・ブロッカー

- `_hb_summary` 変数は heartbeat_end 保存処理内で定義されており、直後の reflection 保存条件で参照している。変数スコープに注意
- cicchi は subordinates（rue/kuro/sora）を持つため、毎回 `heartbeat_subordinate_check` テンプレートが追加注入される。このテンプレート自体は問題ないが、cicchiのHBが他より長くなる原因
- 19:00パイプライン失敗が続く場合は、cicchi cron_logsとrueのactivity_logを確認する

## 変更したファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `core/_anima_heartbeat.py` | heartbeat_end保存前にサマリー正規化（HEARTBEAT_OK切り詰め・O/P/R除去） |
| `core/_anima_heartbeat.py` | heartbeat_reflection保存条件を _hb_summary != HEARTBEAT_OK に変更 |
| `~/.animaworks/animas/*/activity_log/全日付.jsonl` | heartbeat系エントリをHEARTBEAT_OKに置換、heartbeat_reflectionを削除 |
| `~/.animaworks/animas/*/episodes/` | 全削除 |
| `~/.animaworks/animas/*/vectordb/` | 全削除・再作成 |
| `~/.animaworks/animas/*/index_meta.json` | {} にリセット |
| `~/.animaworks/animas/*/shortterm/chat/archive/` | 全削除 |
| `~/.animaworks/animas/*/state/conversation.json` | リセット |
| `.claude/.../memory/MEMORY.md` | クリーンアップ手順を完全版に更新（episodes/vectordb/heartbeat_reflection追加） |
