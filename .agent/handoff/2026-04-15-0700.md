# HANDOFF - 2026-04-15-0700

## 使用ツール
Claude Code Sonnet 4.6（VSCode拡張版）

## 作業対象プロジェクト
- `~/Projects/animaworks` — AnimaWorks AIエージェントフレームワーク

---

## 現在のタスクと進捗

### TikTok画像生成パイプライン修復
- [x] `tiktok_image.py`: `fal_client` ライブラリ依存を除去、`FalTextToImageClient`（httpx直接）に統一
- [x] `pyproject.toml`: `pillow>=12.1.1` を dependencies に追加（`uv add Pillow`）
- [x] `uv.lock` 更新
- [x] `.claude/settings.json`: `effortLevel: max`、`CLAUDE_CODE_DISABLE_ADAPTIVE_THINKING: 1` 追加
- [x] コミット・プッシュ完了（`fork` remote）
- [x] サーバー再起動済み
- [x] tama が画像5枚生成・投稿・Telegram通知まで正常完了を確認

### tiktok_fetch_news URL問題修正
- [x] Google News RSSフィードを直接記事URLを返すフィードに差し替え
  - 旧: `news.google.com/rss/...`（CBMi形式のリダイレクトURLのみ返す）
  - 新: ITmedia AI / AINOW / Zenn / Gihyo の直接RSS
- [x] コミット・プッシュ完了
- [x] サーバー再起動済み・動作確認済み（直接URLが返ることを確認）

---

## 試したこと・結果

- ✅ `FalTextToImageClient` への切り替え → fal-client ライブラリ不要になり安定
- ✅ `uv add Pillow` → pyproject.toml に永続化、uv sync後も消えない
- ✅ tama が venv Python 直接実行スクリプトで画像生成を回避成功（MCP経由不要）
- ✅ Google News RSS → ITmedia/AINOW/Zenn/Gihyo 直接RSS → 直接記事URL取得成功
- ❌ MCPツール ToolSearch で `mcp__aw__tiktok_generate_carousel_images` が空を返す問題 → **未解決だが対応不要と判断**（tamaが直接実行で回避）
- ❌ Google News RSSのHTTPリダイレクト追跡 → Googleの仕様でJS経由のため解決不可

---

## MCPツールToolSearch問題（未解決・対応不要）

**現象**: tamaのセッションで `ToolSearch` が全MCPツールに対して空を返す（4/12から）
**調査結果**: MCPサーバー自体は正常（25ツールを正しくロード）。Claude Code SDK側の動作変化の可能性。
**対応方針**: tama が venv Python 直接実行で回避できているため、修正は不要と判断。

---

## 次のセッションで最初にやること

特になし（前セッションの持ち越し事項はすべて完了）

---

## 注意点・ブロッカー

- `tiktok_image.py` の `_generate_single()` は MCP 経由ではなく venv Python 直接実行が現在の運用
- Zenn RSSは全技術記事が対象のためAIフィルタで絞る必要あり（`AI_FILTER_WORDS` が機能）
- `.claude/settings.json` の `effortLevel: "max"` はIDEが警告を出すが意図通り
