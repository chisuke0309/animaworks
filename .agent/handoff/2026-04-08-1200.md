# HANDOFF — 2026-04-08 12:00

## 使用ツール
Claude Code (claude-sonnet-4-6)

---

## 今回のセッションで実施した内容

### 1. x_post.py — 二重投稿防止の恒久修正
- **問題**: `execute_pending_posts` が同一スロットの承認済み全件をポストしていた
- **修正**: `core/tools/x_post.py` — 承認済みを全収集後、最古1件のみ実行（`approved_files[:1]`）
- 2件以上あれば Warning ログにスキップIDを記録

### 2. ticket_manager.py — 人間アカウント宛の未引取アラート除外
- **問題**: yomi→chisuke 等、人間アカウント宛メッセージで「⚠️ メッセージ未引取」アラートが飛び続けていた
- **修正**: `core/supervisor/ticket_manager.py`
  - `_is_human()` メソッド追加: `~/.animaworks/animas/<name>/status.json` の `enabled: false` で判定
  - タイムアウト検出時、受信者が人間ならアラートを送らずタイムアウト状態にだけ更新

### 3. AGENTS.md — 人間アカウント追加時の注意事項追記
- `enabled: false` が必須な理由（未引取アラート誤報防止 + asset_reconciler誤課金防止）をチェックリスト直下に記載

### 4. HBトークン消費の調査（修正未着手）
調査により以下の事実を確認：

**processedフォルダ問題**:
- `~/.animaworks/shared/inbox/cicchi/processed/` に359件（2026-03-09〜）が蓄積
- 自動削除/ローテーション機能がコードベースに存在しない
- cicchiがBashで `ls inbox/` を叩くとprocessed/の全件が出力に含まれてLLMに渡される

**heartbeat.mdのinbox確認指示問題**:
- cicchiのheartbeat.mdに「Bashで `ls shared/inbox/cicchi/` を実行せよ」と明示されている
- EISDIRエラー回避のワークアラウンドとして書かれたと推測
- しかしinboxの監視・配送はシステム（`_anima_inbox.py`）が自動で行うため、HBで確認する必要自体がない
- → heartbeat.mdのinbox確認ブロックごと削除が妥当

**ToolSearch14回失敗**:
- `read_dm_history` 等、HBで使えないツールを毎回ToolSearchで探して全件失敗
- heartbeat.mdかinjection.mdに古い参照が残っていると推測（未確認）

**トークン記録なし**:
- activity_logのheartbeat_start/endにトークン数が記録されていない
- system_promptは3730〜3731文字（小さい）
- 膨らんでいるのはtool_result（Bashの出力）側

---

## 未完了・次回の確認ポイント

- [ ] **knowledge_lint.py のKeyErrorバグ修正** — `check_workflow_consistency` で `rue` のKeyError（前回から持ち越し）
- [ ] **call_human attachments修正の動作確認** — maruがTelegram画像添付できるか（前々回から持ち越し）
- [ ] **processedフォルダ自動ローテーション実装** — N日以上古いファイルを定期削除するジョブ
- [ ] **cicchi heartbeat.md のinbox確認ブロック削除** — 不要なBash ls + ToolSearch消費を排除

---

## 変更ファイル一覧

| ファイル | 変更内容 |
|---------|---------|
| `core/tools/x_post.py` | execute_pending_posts: 最古1件のみ実行に変更 |
| `core/supervisor/ticket_manager.py` | _is_human()追加、人間宛アラート除外 |
| `AGENTS.md` | 人間アカウント追加時の注意事項追記 |

---

## モデル構成

| anima | モデル | ロール |
|-------|--------|--------|
| cicchi | claude-sonnet-4-6 | X事業部オーケストレーター |
| rue | claude-sonnet-4-6 | ニッチ調査 |
| kuro | claude-haiku-4-5 | コンテンツ制作 |
| sora | claude-haiku-4-5 | ビジュアル生成 |
| hana | claude-haiku-4-5 | エンゲージメント担当 |
| maru | claude-sonnet-4-6 | TikTok事業部リーダー |
| chiro | claude-haiku-4-5 | トレンド調査 |
| tama | claude-sonnet-4-6 | カルーセル制作 |
| yomi | claude-sonnet-4-6 | general |
| chisuke | — (disabled) | 人間オーナー |
| tomo | — (disabled) | 人間オーナー |

---

## Knowledge Lint レポート

**サマリ**: 実行失敗（`check_workflow_consistency` で `rue` の KeyError）

### Critical Issues
- knowledge_lint.py 自体がクラッシュするため全件未評価
- バグ箇所: `scripts/knowledge_lint.py` L619 `role_mentions[anima]` — `rue` がキーとして存在しない
- **次回セッションで修正が必要**

### 前回（2026-04-07）のWarning参考値
- char_limit: 3件（280文字制限の記述）
- workflow: 1件（rueのinjection.mdにコンテンツ制作の役割記述）
