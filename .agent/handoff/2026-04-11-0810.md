# HANDOFF — 2026-04-11 セッション

## 使用ツール
Claude Code（Sonnet 4.6）

## 作業対象プロジェクト
animaworks（TikTokエンゲージメント追跡・通知改善）

---

## 現在のタスクと進捗

### ✅ tiktok_record_post ツール追加（タイトル別管理）

**目的**: maru が納品するときのタイトルを `post_plan_log.jsonl` に永続保存し、`tiktok_engagement.jsonl` との前方マッチングに使えるようにする。

**実装内容**:
- `core/tools/tiktok_analytics.py` に `tiktok_record_post` ツールを追加
  - `title`（必須）= call_human の subject と同じ値
  - `format_type`（任意）= チェックリスト型/手順型など
  - `slot`（任意）= 朝/夕方
  - 保存先: `~/.animaworks/animas/maru/knowledge/post_plan_log.jsonl`
- `maru/procedures/carousel-content-pipeline.md` Step 4 を更新
  - `call_human` 成功後に `tiktok_record_post` を必ず呼ぶ指示を追記
  - `title` = subject と完全同一の文字列であることを明記

**マッチング方針**: `post_plan_log.jsonl`.title ←→ `tiktok_engagement.jsonl`.title を日本語前方マッチングで紐付ける。実際のデータが溜まったら一致度を確認し、必要なら `caption_prefix` フィールドを追加。

---

### ✅ Telegram通知フォーマット改善

**変更内容** (`core/notification/channels/telegram.py`):
- タイトルを本文と別メッセージで送信するように変更（3メッセージ構成）
  1. タイトルメッセージ: `✨ **[subject]**`（コピーしやすい単行）
  2. 本文メッセージ: キャプション本文
  3. 画像メッセージ: アルバム形式（既存）
- urgent/high 時: `🚨 **[URGENT] [subject]**`

**変更前**: 1メッセージに `**subject**\n\nbody` をまとめていた（地味、コピー不便）

---

### ✅ 保存数（saves）取得方針の決定

- TikTok Studio UI に保存数列なし（確認済み）
- 公開動画ページに🔖は表示されるが、1動画1リクエストの追加実装コスト大
- **chisuke の判断**: 実装しない。**views / likes / comments を主指標**に変更
- maru がナレッジファイル（tiktok_strategy.md, account-context.md）を自ら更新済み
  - 「保存されるコンテンツを作る設計方針は維持、数値評価からは除外」

---

### ✅ cron.md 修正（前セッション引き継ぎ）

`~/.animaworks/animas/maru/cron.md` の `tool: tiktok_scrape_engagement` 修正は保持済み。06:30の自動スクレイピングは正常動作中（04-10分データ確認済み）。

---

## 変更ファイル一覧

| ファイル | 変更 |
|----------|------|
| `core/tools/tiktok_analytics.py` | `tiktok_record_post` ツール追加 |
| `core/notification/channels/telegram.py` | タイトル別送信・絵文字追加 |
| `~/.animaworks/animas/maru/procedures/carousel-content-pipeline.md` | Step 4 に `tiktok_record_post` 呼び出し指示追加 |
| `~/.animaworks/animas/maru/cron.md` | 前セッション修正を保持（変更なし） |

---

## 次のセッションで最初にやること

1. **post_plan_log.jsonl の中身を確認**  
   maru が数回納品したら `~/.animaworks/animas/maru/knowledge/post_plan_log.jsonl` に実データが入っているか確認する

2. **タイトル前方マッチングの検証**  
   `post_plan_log.jsonl`.title と `tiktok_engagement.jsonl`.title が一致するか確認。ずれていたら `caption_prefix` フィールドを追加検討

3. **knowledge_lint の実行**  
   本セッションでは未実行。`scripts/knowledge_lint.py` を走らせて知識ファイルの状態確認を推奨

---

## 注意点・ブロッカー

- `post_plan_log.jsonl` はまだ空。maruが次に納品するタイミングで初エントリが入る
- maru が `tiktok_record_post` を call_human と同時に忘れず呼ぶかどうかは、次回納品時に確認
---

## モデル構成

| anima | モデル | ロール |
|-------|--------|--------|
| maru | claude-sonnet-4-6 | TikTok事業部リーダー |
| cicchi | claude-sonnet-4-6 | X事業部オーケストレーター |
| tama | claude-sonnet-4-6 | カルーセル制作 |
| chiro | claude-haiku-4-5 | トレンド調査 |
| kuro | claude-haiku-4-5 | コンテンツ制作 |
| sora | claude-haiku-4-5 | ビジュアル生成 |
| hana | claude-haiku-4-5 | エンゲージメント担当 |
| rue | claude-sonnet-4-6 | ニッチ調査 |
| yomi | claude-sonnet-4-6 | general |
