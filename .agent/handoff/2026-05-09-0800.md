# HANDOFF — 2026-05-09 08:00

## 使用ツール

Claude Code（VSCode拡張・Opus 4.7 1M context）

## 作業対象プロジェクト

**animaworks / TikTok 事業部 maru チーム** — 5/9朝枠の事故対応＋品質チェックパイプライン構造修正

## 現在のタスクと進捗

- [x] 5/9朝枠「画像とJSON乖離」事故の原因究明
- [x] 既存画像10ファイルのバックアップ → FLUX で5枚再生成 → Pillowオーバーレイ焼き直し
- [x] 修正版画像5枚を Telegram 高優先度で chisuke に再送
- [x] 構造修正：`tiktok_verify_image_freshness` ツール新規実装（80行）
- [x] maru/permissions.md / maru/injection.md / tama/knowledge/carousel_production_rules.md 更新
- [x] knowledge_lint.py の `VALID_TOOL_NAMES` 辞書を補強
- [x] サーバー再起動（全8 anima 稼働確認）
- [ ] 5/9朝枠の修正版を chisuke スマホで TikTok 投稿（手動・残作業）

## 試したこと・結果

- ✅ 既存PNG mtime と JSON `saved_at` の比較で stale 判定 — `tiktok_verify_image_freshness` の核ロジックとして採用
- ✅ FLUX 背景再生成＋Pillow焼き直しで5枚すべて最終JSON通りに修正成功（slide_1「自社は大丈夫?」も？付き）
- ✅ HumanNotifier から anima_name="maru" 指定で Telegram 再送成功（`.env` を `set -a; source .env` で先に読み込む必要あり）
- ✅ knowledge_lint で私の編集分が起こした critical を 0 に収束（残20件は既存問題）
- ❌ 初回 Telegram 再送が「bot_token_env not configured」で失敗 — 原因は `uv run python` が `.env` を自動ロードしないため。明示的に環境変数を export する必要あり
- ❌ tama 側の手順書に「差し戻し時は画像も再生成必須」が無く、JSONだけ修正→画像古いまま納品の事故が構造的に発生していた → 今回 `carousel_production_rules.md` に「差し戻し対応プロトコル」セクション追加で解決

## 次のセッションで最初にやること

1. 5/9夕方枠 14:00 cron で `tiktok_verify_image_freshness` が自然に走るか観察（差し戻し1回もなければ素通りでOK、stale 検出時の差し戻しが正しく動くか）
2. 06:30 自動スクレイピング失敗（`Playwright Sync API inside asyncio` エラー）の修正

## 注意点・ブロッカー

- **既知の構造的問題**: judge ルーブリック（5項目：単体完結性・主述・比喩・フック強度・ベネフィット主語）は「面白さ・チャンネルらしさ」を捕捉していない。今朝のpass版は平均4.0ぴったりで通ったが内容は機械的（①②③④⑤の羅列）。**Vision ベースの judge への拡張は未着手**
- **画像焼き直しのコスト**: 既存PNGはオーバーレイ込みで上書き保存されているため、overlay_texts 修正時は FLUX 背景再生成（5枚で約 $0.15〜0.25）が必須
- **knowledge_lint の既存 critical 20件**（`tiktok_cookie_status` / `fal_key` / `x_post_approval` / `generate_carousel_images`（旧名）/ `kuro/injection.md` の format_rule 等）は今回の作業と無関係。別セッションで掃除推奨
- **Telegram再送スクリプト** (`/tmp/resend_morning.py`) は単発作成のため一時ファイル。同種の救済が必要になったら再利用可能
- **画像バックアップ**: `~/.animaworks/tiktok_images/draft_20260508_morning/_backup_20260509_073000/` に旧版10ファイル保管（不要になったら削除可）

## 変更ファイル一覧

### リポジトリ内（git管理対象）

- 新規: `core/tools/tiktok_verify_image_freshness.py`（80行）
- 編集: `scripts/knowledge_lint.py`（VALID_TOOL_NAMES に tiktok 系9件追加、NON_TOOL_NAMES に2件追加）
- 編集: `.agent/units/tiktok_unit.md`（5/9朝枠事故と構造修正を反映）
- 新規: `.agent/handoff/HANDOFF.md`（本ファイル）
- アーカイブ: `.agent/handoff/2026-05-08-1627.md`（前回HANDOFF）

### Anima領域（リポジトリ外、`~/.animaworks/`）

- 編集: `animas/maru/permissions.md`（`tiktok_verify_image_freshness: yes` 追加）
- 編集: `animas/maru/injection.md`（品質チェック8項目化、8番（画像鮮度）の運用ルール追加）
- 編集: `animas/tama/knowledge/carousel_production_rules.md`（差し戻し対応プロトコル追加）
- 上書き: `tiktok_images/draft_20260508_morning/slide_*.png`（5枚再生成）
- バックアップ: `tiktok_images/draft_20260508_morning/_backup_20260509_073000/`（旧10ファイル）

## モデル構成

| anima | モデル | ロール |
|-------|--------|--------|
| cicchi | claude-sonnet-4-6 | X事業部オーケストレーター |
| rue | claude-sonnet-4-6 | ニッチ調査 |
| kuro | claude-sonnet-4-6 | コンテンツ制作 |
| sora | claude-sonnet-4-6 | ビジュアル生成 |
| hana | claude-sonnet-4-6 | エンゲージメント担当 |
| maru | claude-sonnet-4-6 | TikTok事業部リーダー |
| chiro | claude-sonnet-4-6 | トレンド調査 |
| tama | claude-sonnet-4-6 | カルーセル制作 |
| judge | claude-opus-4-7 | overlay_texts 採点（`tiktok_overlay_judge` 内部） |

## サーバ・Anima 状態

- launchd `com.animaworks.server` 稼働中（PID 22539、本セッション 7:53 再起動）
- 全8 anima reload 済み（cicchi / chiro / hana / kuro / maru / rue / sora / tama）
- `tiktok_verify_image_freshness` ツール discovery 完了（`TOOL_MODULES` で確認済み）
- TikTok Cookie: 2026-10-14 まで有効

## Knowledge Lint レポート

knowledge_lint.py 実行結果:

**サマリ**: critical 20件、warning 0件、info 0件

ただし**今セッションで持ち込んだ critical はゼロ**。残り20件はすべて前セッションから繰り越しの既存問題（`tiktok_cookie_status`, `fal_key`, `generate_carousel_images`（旧名）, `x_post_approval`, `kuro/injection.md` の format_rule 等）。別セッションで掃除推奨。

## 注意点

- **機密情報なし**: API キー・トークン等は本ハンドオフに記載していない（TELEGRAM_BOT_TOKEN は `~/Projects/animaworks/.env` 参照）
- **画像再生成コスト**: tama が差し戻されるたびに FLUX 5枚（約 $0.15〜0.25）が走る。新しい freshness チェックがリトライループを誘発しないか観察
- **judge → freshness の順番**: maru/injection.md では「judge pass を確認 → 納品直前に freshness 呼ぶ」と規定。順番を逆にすると freshness が古いJSONを fresh と誤判定する可能性あり（saved_atが更新される前にmtimeチェックすると素通り）
