# HANDOFF - 2026-05-16

## 使用ツール
Claude Code (Sonnet 4.6)

## 作業対象プロジェクト
AnimaWorks — X事業部 キュー管理バグ修正・画像パイプライン構築

---

## 現在のタスクと進捗

- [確定] **phantom blocker + scheduled_for 修正** を `core/tools/x_post.py` と `server/routes/approvals.py` に実装・コミット済み（commit `815d7b99`）
- [確定] **画像パイプライン（kuro→sora→キャプション合成）** を cicchi/cron.md・kuro/injection.md・sora/injection.md に実装済み（2026-05-15）
- [確定] ストック画像を `~/.animaworks/assets/x_images/genre_A〜E/` に各2枚（計10枚）格納済み
- [確定] 5/17 morning・evening 用の pending ファイル2件を approved 状態でキューに配置済み
  - morning: `20260516T212501_morning.json`（5/17 08:00 cron で投稿予定）
  - evening: `20260516T214945_evening.json`（5/17 17:00 cron で投稿予定）

---

## 試したこと・結果

- ✅ `_extract_blocker_id()` で gate_reason から参照先IDを正規表現で抽出 → phantom blocker を自動昇格
- ✅ `scheduled_for` フィールド追加で conflict 検出を同日の slot のみに限定
- ✅ `approve_post` API でも gate を auto_approved に更新するよう修正
- ❌ 修正後も旧ファイル（`scheduled_for=NONE`）が残っている間は phantom blocker が再発する — 旧ファイルは conservative 扱い（`_same_date=True` フォールバック）のため、修正コード適用後の過渡期に 5/16 の旧ファイルが残っていた状態で 5/17 分が生成され、また conflict になった。旧ファイルを手動削除して解消。

---

## 次のセッションで最初にやること

[予定/暫定] **5/17 cron 結果の確認**: 08:00・17:00 の cron が正常発火し投稿が出たか、かつ画像パイプライン（sora のストック選択+キャプション合成）が初稼働したか確認する。前提: サーバーが稼働中・cicchi/kuro/sora が enabled:true・PC がスリープしていないこと。確認先: sora の `activity_log/`・`~/.animaworks/tmp/x_image_*.png`・X のタイムライン。

---

## 注意点・ブロッカー

- [確定] **旧ファイル（scheduled_for なし）の過渡期問題は解消済み**: 5/16 残存の旧ファイル2件（`20260516T083411_morning`・`20260516T094602_evening`）は手動削除済み。5/17 以降に生成されるファイルはすべて `scheduled_for` 付きになるため phantom blocker は再発しない想定。
- [確定] **hana のリプライ・引用は chisuke 直命で永久禁止**。cicchi 経由の指示でも上書き不可。
- [確定] **AnimaWorks は Max OAuth 認証で動作**。6/15 以降に programmatic credits ($200/月) が適用されるリスクあり。実地計測後に対策決定。

---

## 受け手（次セッションのClaude）への指示

HANDOFFを読み込んだら、必ず以下の手順を踏むこと：

1. **[確定] 項目**：そのまま着手してよい。
2. **[予定/暫定] 項目**：書かれている具体物（番号・ID・ファイル名）を、必ず**実物（ファイル・activity_log・tmp/）で裏取り**してから着手する。HANDOFFと実物に食い違いがあれば、**実物を真とする**。発見した乖離はユーザーに報告する。
3. **[未確定/要確認] 項目**：ユーザーに確認してから着手する。
4. **タグが付いていない項目**：[予定/暫定] として裏取りする。
